[DEFAULT]

host = github
lib_name = bounce
repo_name = BOUNCE

user = BorjaRequena
description = Certificates of quantum many-body properties assisted by machine learning
keywords = reinforcement learning, semi-definite programming, quantum physics
author = Borja Requena, Gorka Muñoz-Gil, Jordi Tura
author_email = borja.requena@icfo.eu
copyright = Borja Requena
branch = master
version = 2.0.1
min_python = 3.6
audience = Developers
language = English

custom_sidebar = True
# custom_quarto_yml = False

# Add licenses and see current list in `setup.py`
license = apache2
# From 1-7: Planning Pre-Alpha Alpha Beta Production Mature Inactive
status = 5

# Optional. Same format as setuptools requirements
requirements = numpy networkx fastcore torch picos matplotlib tqdm joblib
# Optional. Same format as setuptools console_scripts
# console_scripts = 
# Optional. Same format as setuptools dependency-links
# dep_links = 


# Change to, e.g. "nbs", to put your notebooks in nbs dir instead of repo root
nbs_path = nbs
doc_path = _docs
recursive = True

# Anything shown as '%(...)s' is substituted with that setting automatically
doc_host =  https://%(user)s.github.io
doc_baseurl = /%(repo_name)s/
git_url = https://github.com/%(user)s/%(repo_name)s/tree/%(branch)s/

lib_path = %(repo_name)s
title = %(lib_name)s

#Optional advanced parameters
#Monospace docstings: adds <pre> tags around the doc strings, preserving newlines/indentation.
#monospace_docstrings = False
#Test flags: introduce here the test flags you want to use separated by |
tst_flags = slow 
